# Phase 4 — email sending (password reset, verification)

async def send_verification_email(email: str, token: str) -> None:
    pass


async def send_password_reset_email(email: str, token: str) -> None:
    pass
